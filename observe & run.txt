python -m venv venv
venv\Script\activate
pip install -r requirements.txt

docker-compose up -d
docker build -t my-qdrant .
docker run -p 6333:6333 my-qdrant

uvicorn main:app --reload --port 8000

open browser check :
http://localhost:8000/docs

Global variabel :
    - docs_memory = [] 
    - qdrant = QdrantClient("http://localhost:6333") 
    - USING_QDRANT = True 
    - chain = workflow.compile()

Split class and function : 
    - fake_embed -> EmbeddingService 
    - docs_memory + qdrant -> DocumentStore 
    - simple_retrive -> RetrieverNode 
    - simple_answer -> GeneratorNode 
    - workflow -> RAGWorkflow 
    - endpoint /ask, /add -> Router
